diff --git a/.github/workflows/full-source-snapshot.yml b/.github/workflows/full-source-snapshot.yml
deleted file mode 100644
index 5ebbe27..0000000
--- a/.github/workflows/full-source-snapshot.yml
+++ /dev/null
@@ -1,19 +0,0 @@
-name: Full Source Snapshot
-
-on:
-  pull_request:
-
-jobs:
-  snapshot:
-    runs-on: ubuntu-latest
-    steps:
-      - name: Checkout
-        uses: actions/checkout@v4
-
-      - name: Upload full source snapshot
-        uses: actions/upload-artifact@v4
-        with:
-          name: consultant-deck-full-source
-          path: .
-          include-hidden-files: true
-          if-no-files-found: error
diff --git a/.github/workflows/source-snapshot.yml b/.github/workflows/source-snapshot.yml
deleted file mode 100644
index e66237c..0000000
--- a/.github/workflows/source-snapshot.yml
+++ /dev/null
@@ -1,24 +0,0 @@
-name: Source Snapshot
-
-on:
-  pull_request:
-
-jobs:
-  snapshot:
-    runs-on: ubuntu-latest
-    steps:
-      - name: Checkout
-        uses: actions/checkout@v4
-
-      - name: Upload source snapshot
-        uses: actions/upload-artifact@v4
-        with:
-          name: consultant-deck-source-snapshot
-          path: |
-            SKILL.md
-            scripts/qa_pptx.py
-            scripts/demo_generate_deck.py
-            scripts/create_template.py
-            tests
-            .github/workflows/ci.yml
-          if-no-files-found: error
diff --git a/SKILL.md b/SKILL.md
index c64087f..aeb9623 100644
--- a/SKILL.md
+++ b/SKILL.md
@@ -246,7 +246,10 @@ python scripts/qa_pptx.py <deck.pptx> \
 - source line；
 - action title；
 - 字体 XML；
-- 边界、溢出、重叠、对齐和页码。
+- 画布边界；
+- 启发式文本溢出与独立文本框重叠。
+
+启发式检查不能替代渲染器级 QA。精确对齐、视觉层级、页码连续性和复杂组合图层仍必须通过渲染切图或人工检查确认。
 
 Demo 和 CI 要求零 findings。真实项目 warnings 必须逐项审阅、修复或记录批准理由。
 
diff --git a/references/qa-checklist.md b/references/qa-checklist.md
index c84f007..9652f50 100644
--- a/references/qa-checklist.md
+++ b/references/qa-checklist.md
@@ -112,7 +112,8 @@ These are quality floors, not reasons to split facts artificially. If the topic
 ## Visual QA
 
 - Fonts include both Arial and Microsoft YaHei in XML.
-- Layout stays within margins and no text boxes overlap.
+- Layout stays within margins; automated QA uses conservative text-overflow and independent-textbox-overlap heuristics.
+- Exact alignment, page-number continuity and complex intentional layering still require rendered-image or manual review.
 - Palette uses primary, accent and gray scale only.
 - Charts are PowerPoint-native when possible.
 - Source line and page number are present where required.
diff --git a/scripts/create_template.py b/scripts/create_template.py
index cfba12a..9433b30 100644
--- a/scripts/create_template.py
+++ b/scripts/create_template.py
@@ -1,4 +1,5 @@
 """Generate a reusable consulting template PPTX."""
+import argparse
 from pathlib import Path
 import sys
 from pptx import Presentation
@@ -52,5 +53,18 @@ def generate(output=ROOT / "assets" / "consulting_template.pptx"):
     print(output)
 
 
+def main(argv=None):
+    parser = argparse.ArgumentParser(description="Generate the reusable consulting template PPTX")
+    parser.add_argument(
+        "--output",
+        type=Path,
+        default=ROOT / "assets" / "consulting_template.pptx",
+        help="Output PPTX path",
+    )
+    args = parser.parse_args(argv)
+    generate(args.output)
+    return 0
+
+
 if __name__ == "__main__":
-    generate()
+    raise SystemExit(main())
diff --git a/scripts/demo_generate_deck.py b/scripts/demo_generate_deck.py
index 18cfa16..cc8a535 100644
--- a/scripts/demo_generate_deck.py
+++ b/scripts/demo_generate_deck.py
@@ -1,6 +1,7 @@
 """Generate a data-rich mixed Chinese-English consulting demo deck."""
 from __future__ import annotations
 
+import argparse
 from datetime import date
 from pathlib import Path
 import json
@@ -469,5 +470,20 @@ def generate(output=ROOT / "examples" / "demo_ai_transformation.pptx",
     return output, evidence_path, briefs_path
 
 
+def main(argv=None):
+    parser = argparse.ArgumentParser(description="Generate the data-rich consulting demo deck")
+    parser.add_argument(
+        "--output",
+        type=Path,
+        default=ROOT / "examples" / "demo_ai_transformation.pptx",
+        help="Output PPTX path",
+    )
+    parser.add_argument("--evidence-output", type=Path, default=None, help="Output evidence.json path")
+    parser.add_argument("--briefs-output", type=Path, default=None, help="Output briefs.yaml path")
+    args = parser.parse_args(argv)
+    generate(args.output, args.evidence_output, args.briefs_output)
+    return 0
+
+
 if __name__ == "__main__":
-    generate()
+    raise SystemExit(main())
diff --git a/scripts/qa_pptx.py b/scripts/qa_pptx.py
index d250a10..6e9bc9c 100644
--- a/scripts/qa_pptx.py
+++ b/scripts/qa_pptx.py
@@ -3,7 +3,9 @@ from __future__ import annotations
 
 import argparse
 import json
+import math
 import re
+import sys
 import zipfile
 from dataclasses import asdict, dataclass
 from pathlib import Path
@@ -42,16 +44,25 @@ BANNED_TERMS = {
 EXEMPT_ROLES = {"cover", "section_divider", "divider", "navigation", "agenda"}
 CONCEPT_ROLES = {"conceptual_framework", "concept", "framework"}
 QUALITATIVE_PATTERNS = {
-    "显著": re.compile(r"显著(?:提升|降低|增长|下降|改善|增加|减少)"),
-    "大幅": re.compile(r"大幅(?:提升|降低|增长|下降|改善|增加|减少)"),
-    "明显": re.compile(r"明显(?:高于|低于|提升|降低|增长|下降|改善)"),
-    "快速": re.compile(r"快速(?:增长|提升|下降|扩张|收缩)"),
-    "领先": re.compile(r"领先(?:于|同业|行业|市场)"),
-    "大量": re.compile(r"大量(?:节省|减少|增加|增长)"),
+    # Warning-level recall is intentionally broad: the same statement must contain
+    # a number, range or threshold when these strength signals are used.
+    "显著": re.compile(r"显著"),
+    "大幅": re.compile(r"大幅"),
+    "明显": re.compile(r"明显"),
+    "快速": re.compile(r"快速"),
+    "大量": re.compile(r"大量"),
+    "领先/优于": re.compile(r"(?:领先|优于|远高于|远低于|更好|更高|更低)"),
     "significantly": re.compile(r"\bsignificantly\b", re.IGNORECASE),
